<%= tag.div(class: "hidden ", data: { controller: "hr--text-component", hr__text_component_label_value: @label, hr__text_component_klass_value: @klass, hr__text_component_hr_class_value: @hr_class, hr__text_component_span_class_value: @span_class }) do %>
  <% prepends.each do |prepend| %>
    <%= prepend %>
  <% end %>
  <template>
    <%= content ? content : @label %>
  </template>
  <% appends.each do |append| %>
    <%= append %>
  <% end %>
<% end %>