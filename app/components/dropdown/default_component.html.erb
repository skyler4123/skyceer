<%= tag.div(class: 'hidden ', data: { controller: "dropdown--default-component", dropdown__default_component_klass_value: @klass, dropdown__default_component_trigger_event_value: @trigger_event }) do %>

  <div data-dropdown--default-component-target="header">
    <% if header %>
      <%= header %>
    <% elsif @label %>
      <%= @label %>
    <% else %>
      <%= render(ButtonComponent.new(label: "Default Dropdown")) do |c| %>
        <% c.with_append do %>
          <%= render(IconComponent.new(name: 'chevron-down', klass: "rotate-0 open:rotate-180 duration-200 ease-in-out")) %>
        <% end %>
      <% end %>
    <% end %>
  </div>
  
  <div data-dropdown--default-component-target="body" class="hidden">
    <%= body ? body : "Sample Content!" %>
  </div>
  
<% end %>
