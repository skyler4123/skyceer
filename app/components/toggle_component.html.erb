<%= tag.div(class: 'hidden ', data: {
  controller: @controller,
  action: @controller_action,
  toggle_component_event_value: @event,

  toggle_component_toggle_direction_value: @toggle_direction,

  toggle_component_label_value: @label,
  toggle_component_position_value: @position,
  toggle_component_klass_value: @klass,
  toggle_component_content_class_value: @content_class,
  toggle_component_grid_class_value: @grid_class,
  toggle_component_klass_default_value: @klass_default,
  toggle_component_content_class_default_value: @content_class_default,
  toggle_component_grid_class_default_value: @grid_class_default,
  **@rest_data_attributes
}) do %>
  <div data-toggle-component-target="content">
    <div data-toggle-component-target="grid">
      <% if content? %>
        <%= content %>
      <% else %>
        "Empty Content!"
      <% end %>
    </div>
  </div>
<% end %>