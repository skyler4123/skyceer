<div class="w-full">
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-2xl">Chat conversations</h1>
    <%= link_to "New chat conversation", new_chat_conversation_path, class: "rounded-lg py-3 px-5 bg-slate-800 text-white block font-medium" %>
  </div>

  <div id="chat_conversations" class="min-w-full">
    <% @chat_conversations.each do |chat_conversation| %>
      <%= render partial: 'chat_conversation', locals: { chat_conversation: chat_conversation, chat_messages: chat_conversation.chat_messages } %>
    <% end %>
    <%
=begin%>
 <%= render @chat_conversations %> 
<%
=end%>
  </div>
</div>
