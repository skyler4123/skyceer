json.extract! calendar_group, :id, :user_id, :name, :color, :border_color, :background_color, :drag_background_color, :created_at, :updated_at
json.url calendar_group_url(calendar_group, format: :json)
