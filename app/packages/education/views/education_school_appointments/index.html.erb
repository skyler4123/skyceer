<div class="w-full">
  <% content_for :title, "Education school appointments" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education school appointments</h1>
    <%= link_to "New education school appointment", new_education_school_appointment_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="education_school_appointments" class="min-w-full">
    <% @education_school_appointments.each do |education_school_appointment| %>
      <%= render education_school_appointment %>
      <p>
        <%= link_to "Show this education school appointment", education_school_appointment, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
      </p>
    <% end %>
  </div>
</div>
