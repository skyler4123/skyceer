<div class="w-full">
  <% content_for :title, "Education rooms" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education rooms</h1>
    <%= link_to "New education room", new_education_room_path, class: "rounded-lg py-3 px-5 bg-slate-800 text-white block font-medium" %>
  </div>

  <div id="education_rooms" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_rooms).html_safe %>