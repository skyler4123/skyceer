<div class="w-full">

  <% content_for :title, "Education lessons" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education lessons</h1>
    <%= link_to "New education lesson", new_education_lesson_path, class: "rounded-lg py-3 px-5 bg-slate-800 text-white block font-medium" %>
  </div>

  <div id="education_lessons" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_lessons).html_safe %>