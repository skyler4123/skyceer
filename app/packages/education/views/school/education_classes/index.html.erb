<div class="w-full">
  <% content_for :title, "Education classes" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education classes</h1>
    <%= link_to "New education class", new_education_class_path, class: "rounded-lg py-3 px-5 bg-slate-800 text-white block font-medium" %>
  </div>

  <div id="education_classes" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_classes).html_safe %>