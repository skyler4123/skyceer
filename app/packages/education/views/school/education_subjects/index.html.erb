<div class="w-full">

  <% content_for :title, "Education subjects" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education subjects</h1>
    <%= link_to "New education subject", new_education_subject_path, class: "rounded-lg py-3 px-5 bg-slate-800 text-white block font-medium" %>
  </div>

  <div id="education_subjects" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_subjects).html_safe %>