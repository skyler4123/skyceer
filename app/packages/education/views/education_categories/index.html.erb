<div class="w-full">

  <% content_for :title, "Education categories" %>

  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Education categories</h1>
    <%= link_to "New education category", new_education_category_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div id="education_categories" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_categories).html_safe %>
