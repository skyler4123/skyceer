<div class="w-full">
  <% content_for :title, "Education students" %>
  <div class="flex justify-between items-center">
    <h1 class="font-bold text-4xl">Students</h1>
    <%= link_to "New student", new_education_student_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>

  <div data-<%= view_controller(__FILE__) %>-target="filter"></div>

  <div data-<%= view_controller(__FILE__) %>-target="index" id="education_students" class="min-w-full"></div>
  <%== pagy_nav(@pagy) if @pagy.pages > 1 %>
</div>
<%= content_data(@education_students).html_safe %>