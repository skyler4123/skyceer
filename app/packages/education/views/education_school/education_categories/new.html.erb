<div class="mx-auto w-4/5 mt-10">
  <h1 class="font-bold text-2xl">New category</h1>

  <%= form_with(model: @education_category, class: "contents") do |form| %>

    <div class="my-5">
      <%= form.label :name, class: "required" %>
      <%= form.text_field :name, class: "block shadow rounded-md border border-gray-400 outline-none px-3 py-2 mt-2 w-full" %>
    </div>

    <div class="inline">
      <%= form.submit "Save", class: "rounded-lg py-3 px-5 bg-slate-800 text-white inline-block font-medium cursor-pointer" %>
    </div>
  <% end %>

</div>
