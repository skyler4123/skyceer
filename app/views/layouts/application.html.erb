<!DOCTYPE html>
<html dir="ltr">
  <head>
    <title>Skyceer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="global event" class="flex flex-col justify-center items-center bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-white">
    <%= render(Layout::Application::HeaderComponent.new) %>
    <main class="flex flex-col items-center w-full">
      <%= yield %>
    </main>
    <%= render(Layout::Application::FooterComponent.new) %>
  </body>
</html>
