<% img_event_id = SecureRandom.uuid %>
<%= button(test: true, event: { id: img_event_id, listener: :click, action: :rotate }) %>
<%= render(InputComponent.new(test: true, label: "Label Test", type: :text, name: :name, is_floating_label: :true,
  format_options: {
  date: true,
  delimiter: '-',
  datePattern: ['Y', 'm', 'd']
  }))
%>